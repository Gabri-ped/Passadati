using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Esercizio3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // 2 modi per far si che l'inspector riconosca dei valori che vogliamo ad assegnare ad un
        // Game Object sono: la variabile "int" e la variabile "float". La variabile "int" si utlizza per specificare numeri interi;
        // mentre la variabile "float" si utilizza per specificare numeri decimali.
        // con le variabili deve essere annessa la funzione "public" e tale funzione deve essere scritta fuori da start e update. 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
